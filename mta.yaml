_schema-version: "3.2"
ID: travproctravelprocessor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travproctravelprocessor-destination-content
  type: com.sap.application.content
  requires:
  - name: travproctravelprocessor-destination-service
    parameters:
      content-target: true
  - name: travproctravelprocessor-repo-host
    parameters:
      service-key:
        name: travproctravelprocessor-repo-host-key
  - name: travproctravelprocessor-uaa
    parameters:
      service-key:
        name: travproctravelprocessor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travproctravelprocessor_html_repo_host
          ServiceInstanceName: travproctravelprocessor-html5-service
          ServiceKeyName: travproctravelprocessor-repo-host-key
          sap.cloud.service: travproctravelprocessor
        - Authentication: OAuth2UserTokenExchange
          Name: travproctravelprocessor_uaa
          ServiceInstanceName: travproctravelprocessor-xsuaa-service
          ServiceKeyName: travproctravelprocessor-uaa-key
          sap.cloud.service: travproctravelprocessor
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: travproctravelprocessor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travproctravelprocessor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travproctravelprocessor.zip
      name: travproctravelprocessor
      target-path: resources/
- name: travproctravelprocessor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travproctravelprocessor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travproctravelprocessor-destination-service
    service-plan: lite
- name: travproctravelprocessor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travproctravelprocessor-xsuaa-service
    service-plan: application
- name: travproctravelprocessor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travproctravelprocessor-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
